import { Button } from "@/components/ui/button";
import Image from "next/image";

export default function Home() {
  return (
    <main className="flex h-[100vh] flex-col items-center justify-center bg-gradient-to-r from-orange-800/50 to-slate-200">
      <Button>Click</Button>
    </main>
  );
}
